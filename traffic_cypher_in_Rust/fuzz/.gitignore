# cargo-fuzz writes its build into a `target` sibling of fuzz_targets/
target/
# libFuzzer accumulates new inputs and crashing artifacts here.
# Seed corpora under corpus/<target>/ ARE committed (see !corpus/).
artifacts/
# Lockfile is regenerated by `cargo fuzz`; keep parent Cargo.lock canonical.
Cargo.lock
